The provided Python project is a
simple dice rolling program. It
prompts the user to input the
number of dice they want to roll,
generates random numbers for
each die, and then displays the
individual rolls and their sum. The
program continues to run until the
user decides to exit.